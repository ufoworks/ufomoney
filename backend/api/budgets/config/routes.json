{
  "routes": [
    {
      "method": "GET",
      "path": "/budgets",
      "handler": "budgets.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/budgets/count",
      "handler": "budgets.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/budgets/:id",
      "handler": "budgets.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/budgets",
      "handler": "budgets.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/budgets/:id",
      "handler": "budgets.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/budgets/:id",
      "handler": "budgets.delete",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/budgets/:id/clone",
      "handler": "budgets.clone",
      "config": {
        "policies": []
      }
    }
  ]
}
